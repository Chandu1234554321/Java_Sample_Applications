spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/chandu
    username: root
  jpa:
    show-sql: true
    
    hibernate:
      ddl-auto: update
  batch:
     Jdbc:
       initialize-schema: ALWAYS
     job:      enabled: false
    
server:
  port: 8082
  